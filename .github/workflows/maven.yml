# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    permissions:
      contents: 'read'
      id-token: 'write'
      
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    
  
    - id: 'auth' 
      name: Authenticate with GCP
      uses: google-github-actions/auth@v2
      with:
        workload_identity_provider: 'https://iam.googleapis.com/projects/645586971453/locations/global/workloadIdentityPools/test-identity-pool/providers/test-identity-provider/attribute.repository/semraozturk/WWAutomation'
        service_account: 'test-service-account@imposing-timer-420716.iam.gserviceaccount.com'
        token_format: access_token
        access_token_lifetime: 3600s
        universe: googleapis.com
        access_token_scopes: 'https://www.googleapis.com/auth/cloud-platform'
   
    - name: Access Token 
      run: |
          echo ${{steps.auth.outputs.access_token}}
          echo ${{steps.auth.outputs}}
          echo GOOGLE_APPLICATION_CREDENTIALS="$GOOGLE_APPLICATION_CREDENTIALS"
          cat $GOOGLE_APPLICATION_CREDENTIALS
          echo ${{steps.auth.outputs.project_id}}
          echo ${{steps.auth.outputs.credentials_file_path}}
          echo ${{steps.auth.outputs.auth_token}}
        
    - name: Run API integration tests
      run: |
        mvn test
        
    # Optional: Uploads the full dependency graph to GitHub to improve the quality of Dependabot alerts this repository can receive
    - name: Update dependency graph
      uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6
